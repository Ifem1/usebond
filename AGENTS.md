# Agent notes

Start with `MEGA_PROMPT_FOR_AGENT.md`. Treat it as the finishing brief.

Do not replace the product with a template or reduce the semantic validator to format checking. Preserve the four outcome semantics and finalized-only permit path unless the current GenLayer SDK requires a syntax migration that keeps the same behavior.
